plugins {
    id("dev.kikugie.stonecutter")
}
// Central script — branch-specific build files handle the actual compilation.
// This file exists as the Stonecutter central script for root-level version management.