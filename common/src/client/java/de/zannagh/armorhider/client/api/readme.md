de.zannagh.armorhider.client.api contains records and interfaces that are not game-version dependent and can be used safely in client-side code to change Armor Hider's behavior.

Each interface has a default implementation which Armor Hider uses by default.