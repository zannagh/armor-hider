package de.zannagh.armorhider.client.rendering;

// No longer needed — arm visibility is now handled by forcing ModelPart.visible = true
// in LivingEntityRendererMixin, rather than re-rendering arms with custom RenderTypes.
public final class ArmRenderTypes {
    private ArmRenderTypes() {}
}
